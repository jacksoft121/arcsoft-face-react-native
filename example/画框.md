合并内容总结：
1.
Android 逻辑 (来自 ArcsoftFaceTest识别框对准.tsx):
◦
Frame 旋转：rotatedFrameW = frameH, rotatedFrameH = frameW (270度旋转)。
◦
坐标映射：
▪
x = frameH - (top + h)
▪
y = frameH - (left + w)
▪
交换 w, h
◦
注意：这里 y 的计算使用了 frameH，这在逻辑上看起来是混用了长短边（通常 left 对应长边 frameW），但既然你确认这个文件是 Android 正常对准 的，我就严格保留了这个逻辑。
2.
iOS 逻辑 (来自 ArcsoftFaceTest_ios画框 ok.tsx):
◦
Frame 旋转：rotatedFrameW = frameH, rotatedFrameH = frameW (90度旋转)。
◦
坐标映射：
▪
x = frameH - (top + h)
▪
y = left
▪
交换 w, h
现在 ArcsoftFaceTest.tsx 应该能同时在 Android 和 iOS 上正常工作了。请进行最终测试。
